# create-l

## create-l (l means lib). Simple scaffold to create library.

With NPM:

```bash
$ npm create l@latest
```

With Yarn:

```bash
$ yarn create l
```

With PNPM:

```bash
$ pnpm create l
```

Then follow the prompts!